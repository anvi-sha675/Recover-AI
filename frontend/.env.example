# The frontend talks to the backend via a Vite dev-server proxy (see vite.config.js),
# so no environment variables are required for local development.
# For production deployment, set VITE_API_BASE to the deployed backend URL and
# update src/lib/api.js to use it instead of the relative "/api" path.
VITE_API_BASE=http://localhost:4000
